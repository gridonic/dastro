---
import type { ClassValue } from '../component.types.ts';

interface Props {
  tagName?: keyof HTMLElementTagNameMap;
  class?: ClassValue;
}

const { tagName: TagName = 'div', class: classNames, ...rest } = Astro.props;
---

<!-- @see: https://github.com/datocms/astro-datocms/tree/main/src/ContentLink#data-datocms-content-link-boundary -->
<TagName data-datocms-content-link-boundary class:list={classNames} {...rest}>
  <slot />
</TagName>
